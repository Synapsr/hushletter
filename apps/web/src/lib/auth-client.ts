// Client-side auth configuration
// Full setup will be implemented in Story 1.2 after Convex is initialized

// Placeholder exports - will be replaced with actual auth client in Story 1.2
export const authClient = null

// Auth hooks will be exported here after configuration:
// import { createAuthClient } from "better-auth/react"
// import { convexClient } from "@convex-dev/better-auth/client/plugins"
// export const authClient = createAuthClient({ plugins: [convexClient()] })
// export const { useSession, signIn, signOut, signUp } = authClient
