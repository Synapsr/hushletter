# Convex Configuration
# Get these values from your Convex dashboard at https://dashboard.convex.dev
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Better Auth Secret (generate with: openssl rand -base64 32)
# Set in Convex with: npx convex env set BETTER_AUTH_SECRET=<your-secret>

# Primary app origin used by Better Auth and backend CORS
# Set in Convex with: npx convex env set SITE_URL=https://your-domain.com

# Optional extra trusted origins for Better Auth + CORS (comma-separated)
# Useful for local dev against prod auth deployment
# Set in Convex with:
# npx convex env set --prod AUTH_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: Override the recent-unread section window (in days).
# Defaults: all unread on dev deployments (`CONVEX_DEPLOYMENT=dev:*`), 7 days on prod.
# Set in Convex with:
# npx convex env set RECENT_UNREAD_WINDOW_DAYS=30

# Optional: disable verbose recent-unread diagnostics in Convex logs.
# Diagnostics are currently enabled by default unless this is set to 0.
# npx convex env set DEBUG_RECENT_UNREAD=0

# Optional: Discord webhook URL for in-app "Send feedback" submissions
# Set in Convex with:
# npx convex env set DISCORD_FEEDBACK_WEBHOOK_URL=https://discord.com/api/webhooks/...
